<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sample.query.Sample1">
	
	<!-- board 테이블의 로우정보를 받아옴 -->
	<select id="getBoardCnt" resultType="java.lang.String">
		SELECT count(*) as CNT FROM board
	</select>
	
	<select id="getBoard" resultType="java.util.HashMap">
		SELECT * FROM board
	</select>
	
	<insert id="insertBoard" parameterType="java.util.HashMap" >
		INSERT INTO board(title,content) VALUES(#{title},#{content})
	</insert>
	
</mapper>